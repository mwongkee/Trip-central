// The demo seed lives in @tripboard/shared so the web app and the server seed
// script (scripts/seed.ts) stay in sync. Re-exported here for local imports.
export { seedTrip, seedMembers, seedChildren, seedItems } from '@tripboard/shared';
